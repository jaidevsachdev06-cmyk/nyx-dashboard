import { Wallet } from "ethers";
import { WalletClient } from "viem";
import { JsonRpcSigner } from "@ethersproject/providers";
import { IAbstractSigner } from "./isigner";
/**
 * Takes in a chainId and an Ethers Wallet or an Ethers JsonRpcSigner or a Viem WalletClient
 * Returns an AbstractSigner
 * @param chainId
 * @param signer
 * @returns
 */
export declare function createAbstractSigner(chainId: number, signer: Wallet | JsonRpcSigner | WalletClient): IAbstractSigner;
