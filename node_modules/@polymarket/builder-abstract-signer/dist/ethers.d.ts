import { Wallet } from "ethers";
import { IAbstractSigner } from "./isigner";
import { JsonRpcSigner, Provider } from "@ethersproject/providers";
import { Transaction } from "./types";
export declare class EthersSigner implements IAbstractSigner {
    readonly chainId: number;
    readonly provider: Provider;
    readonly signer: Wallet | JsonRpcSigner;
    constructor(chainId: number, signer: Wallet | JsonRpcSigner);
    signMessage(message: string): Promise<string>;
    signRawMessage(message: any): Promise<string>;
    signTypedData(domain: any, types: any, value: any, _?: string): Promise<string>;
    getAddress(): Promise<string>;
    estimateGas(tx: Transaction): Promise<bigint>;
    signTransaction(tx: Transaction): Promise<string>;
    sendTransaction(tx: Transaction): Promise<string>;
    waitTillMined(txHash: string): Promise<any>;
}
